# LaProtectora
F200

check django version:
´´´bash
python -m django --version
´´´
if not installed:

pip install django


To clone repository:

git clone https://github.com/ivancarrascoq/LaProtectora
